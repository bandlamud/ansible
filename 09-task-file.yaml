- name: file level variables
  hosts: local
  connection: local
  vars_files:
  - course.yaml
  tasks:
  - name: print course info
    ansible.builtin.debug:
      msg: "The Course name is: {{ COURSE }}, the Duration is: {{ DURATION }}, Trainer is {{ TRAINER }}"