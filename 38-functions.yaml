- name: functions
  hosts: local
  connection: local
  vars:
    technologies: Linux,ansible,Git # staring
  tasks:
  - name: default functions
    ansible.builtin.debug:
      msg: "{{ name | default('Balachandra')}}"
  - name: tranforming string to list
    ansible.builtin.debug:
      msg: "{{ technologies | split(',') }}"